class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        k = 0
        for i in range(len(nums)):
            c = target - nums[i]
            k = i
            for t in nums:
                if t == c and k!= nums.index(t):
                    return [k,nums.index(t)]
